const project = require('./project');
const project_projectImage = require('./project_projectImage');
const workpackage = require('./workpackage');

module.exports = {
  project,
  project_projectImage,
  workpackage};
